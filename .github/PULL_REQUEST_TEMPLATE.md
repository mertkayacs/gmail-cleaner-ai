## What this PR changes

One or two sentences.

## Why

The problem this solves or the behavior this enables.

## How to verify

- [ ] `python -m pytest tests/` passes
- [ ] If touching `triage.py` or `lib/classifier.py`: regression test added
- [ ] If touching the UI: smoke-tested locally with `streamlit run app.py`
- [ ] No new dependencies, or the new dependency is justified in this PR

## Notes for reviewers

Anything subtle. Trade-offs you considered. Things you deliberately left out of scope.
