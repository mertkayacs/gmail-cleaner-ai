python-dotenv>=1.0.0
streamlit>=1.30.0

# Provider SDKs. Install only the ones you actually use.
# (All four are listed so `pip install -r requirements.txt` covers any LLM_PROVIDER.)
anthropic>=0.30.0
openai>=1.50.0
google-generativeai>=0.8.0
requests>=2.31.0
